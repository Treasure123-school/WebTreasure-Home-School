services:
  - type: web
    name: school-backend
    env: node
    plan: free
    rootDir: server  # Tell Render to use the server directory
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
